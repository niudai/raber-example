import { Button, Link, extendTheme, withDefaultColorScheme } from "@chakra-ui/react";
import { BuilderComponent, getPage } from "@raber/react";

export default ({ data }) => (
	<main>
		<h1>Welcome to Next.js</h1>
		<Link variant={'solid'}>Purple</Link>
		
	</main>
)
  
